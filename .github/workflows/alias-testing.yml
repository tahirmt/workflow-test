name: Alias Testing

on:
  push:

jobs:
  test-alias:
    runs-on: macos-10.15
    
    steps:
      - run: |
          # create alias
          alias homels='ls $HOME'

      - name: Test it out
        run: |
          echo "testing out the alias"
          homels
          
      - name: Test export
        run: export TEST_VARIABLE="hello world"
        
      - name: Print exported
        run: echo $TEST_VARIABLE
